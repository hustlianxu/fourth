# 水印相机小程序 · 测试手册（不发布）

> 如果你只是想**在自己的手机上测试**，或者让少量同事参与体验，**不需要注册企业主体、不需要发布**。微信小程序官方提供了多种「**内测 / 体验**」模式，完全可以满足小范围测试。

本手册按 **"最快 → 最自由"** 的顺序给出 4 种方案，你可以按需选择。

---

## 方案一：测试号 · 扫码即测（最快，无需注册）

**推荐度：★★★★★**  
使用微信官方「测试号」，不需要注册小程序账号，不需要 AppID，下载代码后 5 分钟即可在手机上跑起来。

### 1.1 操作步骤

1. 下载本项目代码（克隆仓库或下载 ZIP）。
2. 打开 **微信开发者工具**，登录你的微信账号（任意微信即可）。
3. 选择菜单「**+ 导入项目**」或点击「导入」按钮。
4. 填写项目信息：
   - **项目目录**：选择 `watermark-camera` 目录（即 `app.json` 所在的目录）
   - **AppID**：**直接点「测试号」**，系统会自动生成一个临时 AppID
   - **项目名称**：`水印相机-测试`（任意命名）
5. 点击「导入」。

导入后，开发者工具左侧「模拟器」可以直接看到首页（绿色横幅 + 模板选择卡片 + 拍照按钮）。

### 1.2 让手机也能测试（预览码）

1. 开发者工具顶部点击「**预览**」按钮。
2. 弹出生成一个二维码。
3. 用**你的微信**扫一扫该二维码，手机上会直接打开小程序（标题带「预览版本」字样）。
4. 二维码 25 分钟内有效，过期可重新生成。
5. 可以无限次重新生成，每天无次数上限（对个人测试而言完全够用）。

### 1.3 特性与限制

- ✅ 真机可使用相机、相册、定位
- ✅ 自动获取全部基础接口能力
- ❌ 只能**你自己**的微信扫这个码能打开（同账号登录）
- ❌ 生成的二维码仅 25 分钟有效
- ❌ 不具备正式发布能力（测试号只能用于测试）

如果只你一个人测试，**这是最快方案**。

---

## 方案二：正式 AppID · 预览码 / 体验成员（可多人测试）

**推荐度：★★★★☆**  
注册一个小程序账号（免费），然后把你的同事 / 客户加入「体验成员」，他们可以在**不发布**的情况下体验完整小程序。

### 2.1 注册一个小程序账号

1. 访问 [微信公众平台](https://mp.weixin.qq.com/) 注册一个**小程序账号**（个人免费）。
2. 完成邮箱验证与主体信息填写（需要身份证正反面；个人账号一般 5 分钟完成）。
3. 进入小程序后台 →「**开发管理 → 开发设置**」→ 记录下 **AppID**（形如 `wx1234567890abcdef`）。

### 2.2 把 AppID 写入项目

在 `project.config.json` 第 48 行：

```json
"appid": "wx1234567890abcdef"
```

把原来的 `touristappid` 改成你的 AppID，保存后重新编译。

### 2.3 自测（预览 / 真机调试）

- **预览**：生成二维码，扫码即测（每次生成 25 分钟内有效，可多次生成）。
- **真机调试**：会额外生成调试信息，可以在开发者工具里看到 Console 日志、网络请求、Storage 等，适合排查问题。
- **调试器**：开发者工具右上角「调试器」→ 相当于浏览器 DevTools，可以打断点、查看元素、查看 Network。

### 2.4 邀请同事作为体验成员

**需要注册了正式 AppID 才能邀请体验成员**，不要求上线 / 发布。

1. 进入微信公众平台小程序后台 →「**管理 → 成员管理**」。
2. 点击「**添加体验成员**」，填写对方的**微信号**（或注册邮箱绑定的微信号）。
3. 对方在微信里收到系统消息：「你已被添加为某某小程序的体验成员」。
4. 你在开发者工具点击「**预览**」生成二维码，对方扫码即可。
5. 也可以在小程序后台 →「**版本管理 → 开发版本**」→「生成体验版」→ 把二维码发给对方。

### 2.5 体验成员人数

- 个人小程序最多 **15 人** 体验成员
- 企业小程序最多 **50 人** 体验成员

超出名额时，需要删除旧的体验成员再加新人。

### 2.6 体验成员能否永久使用

- 只要你**不删除**该体验成员、不删除开发版本，对方就**一直**可以用（但小程序一旦被你删除，则全部失效）。
- 生成的体验版二维码长期有效；但每次你上传新版本，建议重新生成一个新二维码让同事更新体验版本。

---

## 方案三：体验版 · 把开发版本固定为体验入口（推荐给客户试用）

**推荐度：★★★★☆**  
如果你想让同事 / 客户**长期测试**某个稳定版本，使用「**体验版**」是最合适的。

### 3.1 操作

1. 在开发者工具中上传一次代码：「上传」→ 填写版本号（如 `0.1.0`）→ 填写项目备注（如「测试用：双语水印模板」）。
2. 登录微信公众平台小程序后台 →「**版本管理 → 开发版本**」。
3. 找到刚才上传的那个版本（列表顶部就是最新的）。
4. 点击右侧「**选为体验版**」。
5. 系统会生成一个长期有效的**体验版二维码**，可以发给同事 / 客户。
6. 把同事 / 客户添加为「体验成员」（如方案二 2.4 所述），否则他们扫码会提示没有体验权限。

### 3.2 切换体验版

每次上传新代码，都可以把最新版本选为「体验版」：

- 原体验版会被替换为新上传的版本；
- 原体验版二维码保持不变，但扫码打开的是新版本。

---

## 方案四：编译版 · 把测试版打包到自定义二维码（高级）

**推荐度：★★☆☆☆**  
适合要发给多个不同测试人员，希望复用同一个二维码的场景（与「体验版」类似）。

### 4.1 自定义编译配置

在开发者工具「项目」菜单里选择「**编译配置**」，可以新增、修改编译模式：

- **启动参数**：打开小程序时直接进入某个页面（例如扫码进入拍照页）。
- **页面参数**：例如 `templateId=handwrite` 让进入拍照页时自动选中某个模板。

示例：

```json
{
  "description": "打开测试水印页面",
  "launchPath": "pages/camera/camera",
  "query": "templateId=handwrite",
  "launchScene": null,
  "navigateToMiniProgramAppIdList": []
}
```

这对内部测试时非常有用，可以直接定位到要测试的功能页。

---

## 测试人员手机上的常见操作

### 手机端如何删除测试数据（清空本地记录）

1. 打开小程序 → 首页右上角「...」→ 点击「设置」
2. 选择「关于水印相机」→ 右上角「设置」
3. 「清除缓存 / 清除数据」或直接长按删除小程序
4. 也可以在小程序内「我的记录 → 清空本地记录」按钮（我们已实现）

### 如何查看调试日志

1. 在开发者工具里打开「真机调试」→ 生成调试二维码
2. 用测试手机扫码，进入真机调试模式
3. 开发者工具右侧 Console 会同步显示手机上的日志

### 测试过程中常用的调试入口

- `pages/index/index`：首页
- `pages/camera/camera`：拍照页（主要测试）
- `pages/list/list`：历史记录列表（数据是否入库）
- `pages/detail/detail`：单条记录详情（图片 + 字段关联）

直接在开发者工具底部 Compile 按钮旁的下拉框切换页面即可。

---

## 测试清单（建议按顺序测试）

把以下项目按顺序在**真机**上测一遍，即可确保功能完整：

- [ ] 首页正常显示模板卡片，点「开始拍照」能进入拍照页
- [ ] 拍照页能成功调用**系统相机**并完成拍摄（iOS 会有授权弹窗）
- [ ] 拍照页「从相册选」可正常取到图片
- [ ] 填写完整数据：货号、西语描述、中文描述、单价、装箱数、件数、体积，全部可正常输入
- [ ] 位置字段「获取定位」能取得经纬度并填充到 watermark
- [ ] 点击「保存水印照片」能成功生成水印图，并跳转到详情页
- [ ] 详情页可以看到完整的图片 + 字段值，点击图片可预览大图
- [ ] 详情页「保存到相册」能把图片写入系统相册（iOS 会提示授权）
- [ ] 回到「我的记录」列表，可以看到刚保存的记录
- [ ] 在详情页点「编辑字段」→ 修改保存后能看到更新后的值
- [ ] 删除某条记录后，列表里对应项应该消失
- [ ] 反复测试 3~5 次，确认每次都有新记录生成，没有重复

---

## 常见问题（测试场景高频问题）

**Q1：真机扫码后一直白屏 / 加载中？**
A：大概率是网络问题，切换一个稳定 Wi-Fi 即可；也可以在开发者工具「详情 → 本地设置」中勾选「不校验合法域名...」方便测试。

**Q2：拍照后预览空白，但上传到手机又正常？**
A：首次使用相机前，iOS 会弹授权请求，请在授权后重新进入拍照页测试；或者改用「从相册选」先做一次测试。

**Q3：生成水印失败 / 报错 `canvasToTempFilePath:fail`？**
A：请先检查「详情 → 本地设置」里的基础库版本，**不低于 2.32**（我们默认在 `project.config.json` 设 `libVersion: "3.0.0"`，足够了）。

**Q4：预览二维码发给同事，他扫码却提示没有权限？**
A：使用的是「正式 AppID」时，需要把同事添加为**体验成员**（方案二 2.4）；或者你使用的是「测试号」，那只能你自己的微信扫。

**Q5：能否让同事直接通过链接下载？**
A：微信小程序没有「下载」概念，只能通过「扫码」或「微信搜索」进入。未发布时只能用「**体验版二维码**」。

**Q6：测试很久后，为什么记录突然没了？**
A：记录通过 `wx.setStorageSync` 保存在本地，**你卸载小程序 / 清理微信缓存**时会一起清除。要持久化存储，需要接入云开发（详见 DEPLOY.md 第七节）。

**Q7：测试阶段如何模拟云端存储与多人同步？**
A：可提前开通云开发（免费版即可），按照 [DEPLOY.md 的第七节](DEPLOY.md#七扩展接入云开发云端存储照片--共享数据) 把本地存储替换为云数据库 + 云存储，这样多个测试员可以看到彼此的记录。

---

## 小技巧：给测试版本一个明显的标识

在 `pages/index/index.js` 里把首页 hero 标题改成「**水印相机（测试版）**」，避免测试人员与未来线上正式版本混淆。

或者，在 `app.json` 中把 `window.navigationBarTitleText` 改为「水印相机-测试」即可。

---

## 从测试 → 正式发布的切换

当你决定上线时，只需要：

1. 把 `project.config.json` 的 `appid` 替换为正式的小程序 AppID（如果还没替换）
2. 上传一次代码 → 填写正式版本号（如 `1.0.0`）
3. 在公众平台提交审核
4. 审核通过后点击「发布」

**完整步骤参考 [DEPLOY.md](DEPLOY.md)。**

祝测试顺利！如在测试阶段遇到具体错误，把「开发者工具的 Console 报错 / 手机截图」发给我，可以进一步定位。
