<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 负责全部 UI，这里只给 Activity 一个透明无标题栏的壳 -->
    <style name="Theme.WatermarkCamera" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
